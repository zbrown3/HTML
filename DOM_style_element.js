let blueElement = document.querySelector('.blue');
blueElement.style.backgroundColor = 'blue'; // selects the first element with a class of blue and assigns blue as the background color.
// Style property of the DOM element provides acces to the inline style of that HTML tag.

document.querySelector('.blue').style.fontFamily = 'Roboto';
// alternate syntax.